export enum tagTypes {
    user = 'users',
    category = 'category',
    subCategory = 'subCategory',
    product = 'product',
}

export const tagTypesList = [tagTypes.user, tagTypes.category, tagTypes.subCategory];
