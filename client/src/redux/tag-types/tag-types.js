// Define an object with tag types
export const tagTypes = {
  user: "users",
  tenant: "tenants",
};

// Create an array containing the values of the properties in the tagTypes object
export const tagTypesList = [tagTypes.user, tagTypes.tenant];
